export function DeviceApprovalPage() {
  return (
    <div className="card">
      <h2>Device approval pending</h2>
      <p>Your account is valid, but this device needs admin approval before access is granted.</p>
    </div>
  );
}
